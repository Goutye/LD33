local class = require 'EasyLD.lib.middleclass'

local Image = class('Image')

function Image:initialize()

end

function Image:draw(x, y, r, sx, sy, ox, oy, kx, ky)

end

function Image:drawPart(mapX, mapY, x, y, w, h, id)

end

return Image